[build]
  cmd = "go build -tags docker -o ./tmp/main ."
  bin = "tmp/main"
  dir = "."
  include_ext = ["go"]
  exclude_dir = ["vendor", "tmp", "testdata"]
  run_after_build = "chmod +x ./tmp/main"

[log]
  level = "debug"