import DashboardLayout from "@/app/dashboard/layout";
import ConfigureApp from "@/components/organisms/Dashboard/ConfigureApp";

const page = () => {
  return (
    <DashboardLayout>
      <ConfigureApp />
    </DashboardLayout>
  );
};

export default page;
