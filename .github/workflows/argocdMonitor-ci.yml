name: CI - ArgoCD-Monitor

on:
  push:
    branches:
      - main
    paths:
      - 'app/server/goServices/ArgoCD-Monitor-Cronjob/**'
  pull_request:
    branches:
      - main
    paths:
      - 'app/server/goServices/ArgoCD-Monitor-Cronjob/**'
      
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.21

      - name: pkg build
        working-directory: ./app/server/goServices/ArgoCD-Monitor-Cronjob
        run: go build -v ./...     
      
      #- name: Run Tests
      #  run: go test ./... 
