name: CI - Slack

on:
  push:
    branches:
      - main
    paths:
      - 'app/server/goServices/Slack/**'
  pull_request:
    branches:
      - main
    paths:
      - 'app/server/goServices/Slack/**'
      
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.21

      - name: pkg build
        working-directory: ./app/server/goServices/Slack
        run: go build -v ./...     
      
      #- name: Run Tests
      #  run: go test ./... 
